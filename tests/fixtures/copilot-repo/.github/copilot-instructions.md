---
name: Copilot Instructions
description: GitHub Copilot custom instructions for this repository
---

# Copilot Instructions

This repository uses TypeScript with strict mode enabled.

## Coding Style

- Use `const` over `let` where possible
- Always type function return values explicitly
- Prefer `async/await` over raw Promises

## Project Context

See `README.md` for full project description.
Refer to `.github/copilot/conventions.md` for naming conventions.
